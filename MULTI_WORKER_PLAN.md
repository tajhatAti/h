# Multi-worker routing — কী দরকার, কী দরকার নেই

তোমার AI-লেখা প্ল্যানটা পড়েছি, আর কোডের সাথে মিলিয়ে দেখেছি।

**সংক্ষেপে: প্ল্যানটা ঠিক আছে, কিন্তু এর ৭০% কাজ ইতিমধ্যেই করা।** নতুন করে
সব বানাতে গেলে যা চলছে তা ভাঙবে। নিচে কোডে যাচাই করা হিসাব।

---

## ১. ইতিমধ্যেই যা আছে

| প্ল্যানে যা চাওয়া | বাস্তবে | কোথায় |
|---|---|---|
| `POST /jobs` — জব চালু | ✅ `POST /internal/jobs` | `runner/app.py:1407` |
| `DELETE /jobs/{id}` | ✅ `DELETE /internal/jobs/{id}` | `:1610` |
| `GET /jobs/{id}/status` | ✅ `GET /internal/jobs/{id}` | `:1564` |
| memory + uptime | ✅ `cpu_pct`, `mem_mb`, `uptime_s` | `_proc_stats()` |
| লগ স্ট্রিম | ✅ SSE | `routes/runspace.py` |
| shared secret auth | ✅ Bearer টোকেন, fail-closed | `_check_secret()` |
| per-job memory ceiling | ✅ RLIMIT | `_set_limits()` |
| per-user ৩ জব | ✅ DB থেকে, worker নির্বিশেষে | `routes/runspace.py:256` |
| একাধিক worker URL | ✅ `RUNNER_SERVICE_URLS` | গতকাল |
| ভরা worker → পরেরটায় | ✅ `X-Runner-Full` | গতকাল |

**তাই "Step 1: worker-API প্যাটার্নে রিফ্যাক্টর" — এটা আর দরকার নেই।
Runner অলরেডি একটা আলাদা HTTP সার্ভিস।**

---

## ২. যা সত্যিই নেই (৩টা)

### 🔴 A. `worker_id` কলাম — এটাই সবচেয়ে জরুরি

**গতকালের কোডে একটা বাগ আছে যেটা আমি এইমাত্র ধরেছি।**

`services/runner_client.py:133`:

```python
targets = pool if creating else pool[:1]
```

নতুন জব pool-এর যেকোনো worker-এ বসতে পারে — কিন্তু restart/stop/log
**সবসময় প্রথম worker-এ** যায়।

চালিয়ে প্রমাণ করেছি:

```
job created on   : https://worker-b.test/internal/jobs
restart sent to  : https://worker-a.test/internal/jobs/job99/restart
```

জব worker-B-তে, restart গেল worker-A-তে। **worker-A বলবে "job not found",
আর সাইট ভাববে জবটা মরে গেছে।**

এখন এটা ঘটছে না, কারণ pool-এ একটাই worker। **কিন্তু তুমি দ্বিতীয় সার্ভিস
যোগ করার সাথে সাথেই ঘটবে।** তাই দ্বিতীয় worker যোগ করার আগে এটা ঠিক করতেই
হবে।

**সমাধান:** `jobs` টেবিলে `worker_url` কলাম। জব তৈরির সময় কোন worker-এ বসল
তা লিখে রাখো; পরের সব কল ওই worker-এই যাবে।

---

### 🟡 B. `/health` লোড রিপোর্ট করে না

এখন যা ফেরত দেয়:

```json
{"status": "ok", "languages": ["python", "javascript", ...]}
```

Least-loaded রাউটিং করতে হলে দরকার: কয়টা জব চলছে, কত RAM ব্যবহার হচ্ছে,
ক্ষমতা কত।

**তবে সৎ কথা:** এটা ছাড়াও কাজ চলে। এখনকার "প্রথম যেটায় জায়গা আছে" পদ্ধতি
২-৩টা worker-এর জন্য যথেষ্ট। Least-loaded তখন দরকার যখন worker-গুলোর
ক্ষমতা আলাদা, বা ৫+ worker হয়।

---

### 🟡 C. Worker অফলাইন হলে "unreachable" দেখানো

এখন worker-এ পৌঁছানো না গেলে জব "offline" দেখায়। কিন্তু জবটা হয়তো **দিব্যি
চলছে** — শুধু সাইট পৌঁছাতে পারছে না। ইউজার ভাববে বট মরে গেছে, অথচ Telegram-এ
বট উত্তর দিচ্ছে।

---

## ৩. যা প্ল্যানে আছে কিন্তু বানানো উচিত নয়

### ❌ Worker registry টেবিল

প্ল্যান বলছে DB টেবিলে worker রাখতে। **দরকার নেই।** worker-এর তালিকা
`RUNNER_SERVICE_URLS` env var-এই আছে — Render-এ একটা লাইন বদলালেই হলো।
টেবিল বানালে UI, CRUD, migration — সব লাগবে, লাভ কিছুই নেই।

`worker_url` কলামটা জবের সাথেই থাক। **Config থাকুক config-এ, ডেটা থাকুক DB-তে।**

### ❌ psutil

প্ল্যান psutil চায়। Runner এখন `/proc` সরাসরি পড়ে — একই তথ্য, শূন্য
dependency। ৫১২MB কন্টেইনারে একটা প্যাকেজ কম মানে একটু বেশি RAM বটের জন্য।

### ❌ Admin panel-এ outage log

আগে দরকার worker আসলেই অফলাইন হয় কিনা দেখা। **০টা outage-এর জন্য ড্যাশবোর্ড
বানানো** — সমস্যা দেখা দিলে তখন বানাও।

---

## ৪. আমার সুপারিশ — ক্রম

| ধাপ | কাজ | কেন | সময় |
|---|---|---|---|
| **১** | `worker_url` কলাম + routing | **এটা ছাড়া ২য় worker যোগ করলেই ভাঙবে** | ছোট |
| **২** | `/health`-এ জব সংখ্যা + RAM | Least-loaded রাউটিং, আর ভরা কিনা বোঝা | ছোট |
| **৩** | "unreachable" আলাদা স্ট্যাটাস | ইউজার যেন ভুল না বোঝে | ছোট |
| ৪ | Least-loaded নির্বাচন | ৩+ worker হলে | মাঝারি |
| ৫ | Registry টেবিল / admin UI | দরকার হলে | — |

**ধাপ ১ এখনই দরকার। ২ আর ৩ ভালো, কিন্তু জরুরি না। ৪ পরে। ৫ সম্ভবত কখনোই না।**

---

## ৫. তোমার প্রশ্নের উত্তর

> "এটা কি ঠিক আছে?"

**প্ল্যানটার দিক ঠিক** — central dispatcher + workers, ঠিক এভাবেই করা উচিত।
আর্কিটেকচার ভুল নয়।

**কিন্তু প্ল্যানটা ধরে নিয়েছে শূন্য থেকে শুরু করছ।** বাস্তবে ৭০% আছে। পুরোটা
নতুন করে বানালে ৭৪৬টা টেস্ট আর যা কিছু চলছে সব ঝুঁকিতে পড়বে — কোনো নতুন
ক্ষমতা না পেয়েই।

**আর একটা জিনিস প্ল্যানে নেই, অথচ সেটাই এখন সবচেয়ে জরুরি** — `worker_id`
বাগটা। AI প্ল্যানটা কোড দেখেনি, তাই ধরতে পারেনি।

> "আমি কি ভালোভাবে বুঝিয়ে বলব?"

দরকার নেই। তুমি যেভাবে বলছ সেটাই যথেষ্ট। **সমস্যাটা বলো, সমাধান না** —
"দুইটা সার্ভার হলে জব হারিয়ে যাবে কিনা" বললেই আমি কোড দেখে বের করব।
বিস্তারিত টেকনিক্যাল স্পেক দিলে আমি সেটা অন্ধভাবে মানতে বাধ্য হই, এমনকি
যখন কোড অন্য কথা বলে।

---

## এখন কী করব?

বলো:

- **"১ করো"** → `worker_url` বাগটা ঠিক করি (আমার সুপারিশ)
- **"১+২+৩ করো"** → তিনটাই একসাথে
- **"পুরো প্ল্যান করো"** → করব, তবে কী কী নতুন করে লেখা হবে আগে বলে নেব
